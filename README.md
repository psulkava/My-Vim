My .vimrc file and other config files.

Run 

`wget -O https://github.com/psulkava/My-Vim/raw/master/autoinstall.sh 
chmod +x autoinstall.sh 
./autoinstall.sh`

Then open up vim and run :PluginInstall 
